base_config: configs/default.yaml
worlds:
  - name: synthetic_dag
    overrides:
      transport:
        solver: masked_sinkhorn
      projection:
        use_aligned: true
  - name: synthetic_dag
    overrides:
      transport:
        solver: unbalanced
      projection:
        use_aligned: true
  - name: synthetic_dag
    overrides:
      projection:
        use_aligned: false
