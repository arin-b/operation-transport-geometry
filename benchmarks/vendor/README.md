# Vendored benchmark dependencies

This directory is reserved for third-party source copies when a public implementation is imported into the benchmark bundle.

Current benchmark adapters use installed public libraries where available, especially POT for transport variants.
